VITE_API_URL=http://localhost:4000/api

# Endpoint y clave publicable del proyecto. La anon key esta pensada para
# vivir en el navegador: no da acceso por si sola, todo pasa por RLS y por
# la verificacion de rol dentro de las funciones RPC.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
